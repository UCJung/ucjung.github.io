# 2026-03-28 개발일지

## 1. 사이트 초기 세팅 및 첫 포스트 작성
- **작업**: Chirpy 테마 기반 GitHub Pages 블로그 생성, _config.yml 한국어/개인정보 설정, 첫 포스트(AI Agent 개요) 작성

---

## 2. 사이트 배포 안 됨
- **현상**: main 브랜치에 push 했으나 사이트가 업데이트되지 않음
- **문제**: GitHub Actions 빌드의 "Test site" 단계에서 실패
- **원인**: _config.yml 정리 과정에서 `jekyll-archives` 설정이 삭제됨. 포스트의 tags(`ClaudeCode`, `AIAgent` 등)에 대한 태그 페이지가 생성되지 않아 htmlproofer가 75개 내부 링크 깨짐을 감지
- **해결**: `jekyll-archives` 설정 복원 (categories, tags 활성화 + permalinks 지정)

---

## 3. 아바타 이미지 누락으로 빌드 재실패
- **현상**: jekyll-archives 수정 후에도 빌드 여전히 실패
- **문제**: htmlproofer가 `/assets/img/avatar.jpg does not exist` 에러 13건 보고
- **원인**: _config.yml에 `avatar: "/assets/img/avatar.jpg"` 경로를 지정했으나 실제 파일이 없었음
- **해결**: 프로필 이미지(profile.png)를 `assets/img/`에 추가하고 config 경로를 `/assets/img/profile.png`로 수정

---

## 4. reveal.js 슬라이드 변환
- **현상**: 기존 HTML 발표자료(`claude_user_guide.html`)를 발표 형태(한 장씩 넘기기)로 보고 싶음
- **작업**: reveal.js 기반으로 변환 (`claude_user_guide_slides.html` 생성)
- **이슈 1**: 폰트 색상이 원본과 달라짐 → reveal.js 기본 CSS가 색상을 덮어씀 → `.reveal` 하위 요소에 원본 색상 강제 적용
- **이슈 2**: 슬라이드 전환 시 하단에 다른 타이틀이 비침 → `overflow: hidden` 추가, 전환 효과를 `fade`로 변경
- **이슈 3**: 6번 슬라이드부터 레이아웃 깨짐 → 변환 스크립트의 `</div>` → `</section>` 치환에서 div 태그 불균형 발생. 스택 기반 파서로 재변환하여 해결

---

## 5. CMS 에디터 추가 (Decap CMS → Sveltia CMS)
- **현상**: 블로그 글을 웹에서 직접 작성/편집하고 싶음
- **작업**: `/admin/` 경로에 Decap CMS 설정
- **이슈 1**: "Login with GitHub" 클릭 시 `api.netlify.com` 으로 리다이렉트되며 "Not Found" → Decap CMS가 기본적으로 Netlify 인증을 사용함. Sveltia CMS(드롭인 대체)로 교체
- **이슈 2**: Sveltia CMS에서도 동일 현상 → `auth_type: pkce`, `app_id` 설정 제거. Sveltia CMS 기본 인증 흐름 사용으로 변경

---

## 6. 테마 변경: Chirpy → Al-folio
- **현상**: 블로그 디자인을 더 근사하게 바꾸고 싶음
- **작업**: Chirpy 테마를 제거하고 Al-folio 테마로 전면 교체. backup-chirpy 브랜치에 이전 상태 보존
- **이슈 1**: CI 빌드 실패 — `requirements.txt` 누락 → Python setup 단계에서 파일을 찾지 못함. `requirements.txt` 추가
- **이슈 2**: CI 빌드 실패 — `about_einstein.md` 참조 오류 → 데모 페이지(profiles, publications 등) 삭제 시 `profiles.liquid`에서 참조하는 파일도 함께 삭제됨. 불필요한 데모 페이지 모두 제거
- **이슈 3**: 배포 후 이전 Chirpy 화면이 보임 → 브라우저 캐시 문제. 강제 새로고침으로 해결. GitHub Pages 소스를 `gh-pages` 브랜치로 변경 필요

---

## 7. Pretendard 폰트 적용
- **현상**: 블로그 폰트를 Pretendard로 변경하고 싶음
- **작업**: `_includes/head.liquid`에 Pretendard CDN 추가, `_sass/_typography.scss`에 `font-family` 설정

---

## 8. 네이비/오렌지 색상 테마 적용
- **현상**: 발표자료(`claude_user_guide.html`)와 동일한 색상 톤으로 블로그를 맞추고 싶음
- **작업**: `_sass/_themes.scss`의 라이트/다크 테마 색상 변수를 발표자료의 색상 팔레트(navy #1A2332, accent #FF6600 등)로 교체
